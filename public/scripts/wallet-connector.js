import { setupBackpack } from './backpack.js';
import { setupMetaMask } from './metamask.js';
import { setupPhantom } from './phantom.js';

export { setupMetaMask, setupPhantom, setupBackpack };

// Este arquivo serve como um ponto central para inicializar as funções de conexão das carteiras.